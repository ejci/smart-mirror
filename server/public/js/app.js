'use strict';

// Declare app level module which depends on filters, and services

angular.module('myApp', ['controllers', 'services', 'components', 'ngRoute', 'ngSanitize', 'ngResource', 'ngAnimate', 'ngCookies']).
    config(function () {
    });

